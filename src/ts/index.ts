import './background';
import './tetragon';
import './menu';
import './overlay';
//import './select';

window.addEventListener('load', () => {
    document.documentElement.classList.add('is--loaded');
});
